public interface IMachine {
    // an interface that represents classes that are a kind of Machine
    public void startMachine();
    public void stopMachine();
    public boolean isRunning();
    public void repair();
}
